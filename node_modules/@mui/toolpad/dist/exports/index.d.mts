import express from 'express';

interface AppHandler {
    handler: express.Handler;
    dispose: () => Promise<void>;
}
interface ToolpadHandlerConfig {
    dev: boolean;
    dir: string;
    base: string;
    externalUrl: string;
    toolpadDevMode?: boolean;
}
declare function createHandler({ dev, dir, base, externalUrl, }: ToolpadHandlerConfig): Promise<AppHandler>;

export { createHandler };
