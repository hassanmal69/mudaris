import * as React from 'react';
import { ToolpadComponents } from '@mui/toolpad-core';
import { R as RuntimeState } from './types-e_iIjmol.mjs';
import { T as ToolpadAppProps } from './ToolpadApp-Bbsx4TB9.mjs';
import '@mui/toolpad-core/appDom';

/**
 * This allows us to hot update the components when a file is added/removed
 */
declare function setComponents(newComponents: ToolpadComponents, pageComponents: Record<string, React.ComponentType>): void;
interface InitParams {
    initialState: RuntimeState;
    base: string;
    ToolpadApp?: React.ComponentType<ToolpadAppProps>;
}
declare function init({ ToolpadApp, initialState, base }: InitParams): void;

export { type InitParams, init, setComponents };
